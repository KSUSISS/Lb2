## Цель работы
Изучить способ использования репозитория в Spring Data JPA и реализовать полноценное CRUD-приложение для управления данными сущности «Школьник».

## Архитектура проекта
- **Стек:** Spring Boot, Maven, Java, Thymeleaf, Bootstrap 5.
- **СУБД:** H2 (встроенная база данных в памяти).
- **Основные слои:** 
  - Entity (Domain): Описание структуры таблицы базы данных (поля, типы данных) средствами Java-класса.
  - Repository: Интерфейс, наследуемый от `CrudRepository`, для выполнения операций чтения и записи в БД без написания SQL-кода.
  - Controller: Класс диспетчера, обрабатывающий HTTP-запросы от браузера, вызывающий методы репозитория и передающий данные в HTML-шаблоны.
  - View (Templates): Пользовательский интерфейс, реализованный с помощью шаблонизатора Thymeleaf и HTML-разметки.
- **Ключевые аннотации:** 
  - Аннотации БД: `@Entity`, `@Table`, `@Id`, `@GeneratedValue`.
  - Аннотации контроллера: `@Controller`, `@GetMapping`, `@PostMapping`.
  - Аннотации параметров: `@PathVariable`, `@ModelAttribute`.

## Алгоритм работы
1. При запуске сервера Spring Boot инициализирует подключение к H2 в оперативной памяти и автоматически генерирует таблицу на основе сущности.
2. При переходе пользователя на корневой адрес контроллер запрашивает все записи из репозитория методом `findAll()` и передает список в шаблон главной страницы.
3. Форма добавления/редактирования обрабатывается через GET-запрос: для новых записей передается пустой экземпляр, для существующих — данные извлекаются методом `findById()` по переданному идентификатору.
4. При редактировании форма сохраняет первичный ключ объекта в скрытом поле `<input type="hidden">`, чтобы при обновлении не создавалась новая запись.
5. Отправка формы инициирует POST-запрос, который сохраняет изменения в БД через метод `save()`.
6. Удаление объекта осуществляется по GET-запросу, содержащему ID в адресной строке. Перед вызовом `deleteById()` программа проверяет наличие объекта в базе с помощью `existsById()`. После выполнения операций происходит переадресация на главную страницу.
