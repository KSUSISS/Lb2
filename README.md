## Цель работы
Изучить способ использования репозитория в Spring Data JPA и реализовать полноценное CRUD-приложение для управления данными сущности «Школьник»[cite: 1].

## Архитектура проекта
- **Стек:** Spring Boot, Maven, Java, Thymeleaf, Bootstrap 5[cite: 1].
- **СУБД:** H2 (встроенная база данных в памяти)[cite: 1].
- **Основные слои:** 
  - Entity (Domain): Описание структуры таблицы базы данных (поля, типы данных) средствами Java-класса[cite: 1].
  - Repository: Интерфейс, наследуемый от `CrudRepository`, для выполнения операций чтения и записи в БД без написания SQL-кода[cite: 1].
  - Controller: Класс диспетчера, обрабатывающий HTTP-запросы от браузера, вызывающий методы репозитория и передающий данные в HTML-шаблоны[cite: 1].
  - View (Templates): Пользовательский интерфейс, реализованный с помощью шаблонизатора Thymeleaf и HTML-разметки[cite: 1].
- **Ключевые аннотации:** 
  - Аннотации БД: `@Entity`, `@Table`, `@Id`, `@GeneratedValue`[cite: 1].
  - Аннотации контроллера: `@Controller`, `@GetMapping`, `@PostMapping`[cite: 1].
  - Аннотации параметров: `@PathVariable`, `@ModelAttribute`[cite: 1].

## Алгоритм работы
1. При запуске сервера Spring Boot инициализирует подключение к H2 в оперативной памяти и автоматически генерирует таблицу на основе сущности[cite: 1].
2. При переходе пользователя на корневой адрес контроллер запрашивает все записи из репозитория методом `findAll()` и передает список в шаблон главной страницы[cite: 1].
3. Форма добавления/редактирования обрабатывается через GET-запрос: для новых записей передается пустой экземпляр, для существующих — данные извлекаются методом `findById()` по переданному идентификатору[cite: 1].
4. При редактировании форма сохраняет первичный ключ объекта в скрытом поле `<input type="hidden">`, чтобы при обновлении не создавалась новая запись[cite: 1].
5. Отправка формы инициирует POST-запрос, который сохраняет изменения в БД через метод `save()`[cite: 1].
6. Удаление объекта осуществляется по GET-запросу, содержащему ID в адресной строке. Перед вызовом `deleteById()` программа проверяет наличие объекта в базе с помощью `existsById()`[cite: 1]. После выполнения операций происходит переадресация на главную страницу[cite: 1].
